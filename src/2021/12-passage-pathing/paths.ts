export const countPathsSingleSmallCaveVisit = (input: string) => {
    // how the hell do you solve this?
    // 1. build data structure: nodes with connecting neighbours (like many linked lists?)
    // see how it was done for the octopus one?
    // go from start and go from 'left most' node
    // base case: reached end or went into a small cave again, or reached start
};
